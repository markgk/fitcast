import { Exercise, Subscriber, Program } from '../Types/GymTypes';

export const makeExersize = (e: Exercise, s: Subscriber): Exercise => {
  return e;
};
export const makeProgram = (p: Program, s: Subscriber): Program => {
  return p;
};
